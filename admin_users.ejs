<!DOCTYPE html>
<html lang="ar" dir="rtl" class="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>النادي الهندسي | <%= pageTitle %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <style>
        .table-header { @apply bg-gray-200 dark:bg-gray-700 text-gray-700 dark:text-gray-200 font-bold uppercase text-sm px-6 py-3; }
        .table-cell { @apply px-6 py-4 whitespace-nowrap text-sm text-gray-800 dark:text-gray-200; }
    </style>
</head>

<body class="bg-gray-50 dark:bg-gray-800 text-gray-800 dark:text-gray-100 min-h-screen font-sans">
    
    <%- include('partials/header', { 
        isLoggedIn: isLoggedIn, 
        isAdmin: isAdmin, 
        currentPage: currentPage 
    }) %>

    <main class="py-12 px-4 md:px-8">
        <div class="max-w-7xl mx-auto">
            <h1 class="text-3xl font-extrabold mb-8 text-blue-800 dark:text-blue-300">
                <%= pageTitle %>
            </h1>

            <div class="overflow-x-auto bg-white dark:bg-gray-700 rounded-lg shadow-xl">
                <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-600">
                    <thead class="bg-gray-50 dark:bg-gray-700">
                        <tr>
                            <th scope="col" class="table-header">الرقم الجامعي</th>
                            <th scope="col" class="table-header">الاسم</th>
                            <th scope="col" class="table-header">القسم</th>
                            <th scope="col" class="table-header">النقاط</th>
                            <th scope="col" class="table-header">اللجنة/الدور</th>
                            <th scope="col" class="table-header">الإجراءات</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-200 dark:divide-gray-600">
                        <% if (users && users.length > 0) { %>
                            <% users.forEach(user => { %>
                                <tr class="hover:bg-gray-100 dark:hover:bg-gray-600">
                                    <td class="table-cell font-mono"><%= user.student_id %></td>
                                    <td class="table-cell font-medium"><%= user.name %></td>
                                    <td class="table-cell"><%= user.department %></td>
                                    <td class="table-cell text-green-600 dark:text-green-400 font-bold"><%= user.points %></td>
                                    <td class="table-cell">
                                        <% if (user.role === 'admin') { %>
                                            <span class="bg-red-100 text-red-800 text-xs font-semibold px-2 py-0.5 rounded dark:bg-red-200 dark:text-red-900">مسؤول</span>
                                        <% } else { %>
                                            <%= user.committee || user.role %>
                                        <% } %>
                                    </td>
                                    <td class="table-cell">
                                        <a href="/admin/users/edit/<%= user.student_id %>" class="text-blue-600 hover:text-blue-900 dark:text-blue-400 dark:hover:text-blue-200 transition">تعديل</a>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="6" class="table-cell text-center text-gray-500">لا يوجد مستخدمون لعرضهم.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>

            <div class="mt-8 text-center text-sm text-gray-500">
                <p>لعرض بيانات المستخدمين، تأكد من وجود بيانات في جدول `users` في قاعدة البيانات.</p>
            </div>
        </div>
    </main>

    </body>
</html>