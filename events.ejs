<!DOCTYPE html>
<html lang="ar" dir="rtl" class="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>النادي الهندسي | الدورات والفعاليات</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    
    <style>
        /* التنسيقات الموحدة (من ملف CSS الخارجي) */
        :root {
            --primary-dark-blue: #0A2F6B; 
            --accent-blue: #06B6D4; /* سماوي */
            --light-bg: #F0F2F5; 
            --main-bg: #FFFFFF;
            --main-text: #333333;
            --soft-text: #6B7280;
        }
        html.dark {
            --light-bg: #1A202C;
            --main-bg: #2D3748;
            --main-text: #E2E8F0;
            --soft-text: #A0AEC0;
            --primary-dark-blue: #90CDF4; /* أزرق فاتح كعنوان في الداكن */ 
            --accent-blue: #38BDF8; /* سماوي أكثر وضوحًا في الداكن */ 
        }

        body { 
            font-family: 'Tajawal', sans-serif; 
            background-color: var(--light-bg); 
            color: var(--main-text); 
            transition: background-color 0.3s, color 0.3s;
        }

        /* تنسيقات مكونات Tailwind باستخدام متغيرات CSS */
        .bg-card { 
            background-color: var(--main-bg); 
            color: var(--main-text); 
            /* تنسيق بطاقة موحد (تم إضافته للمحاكاة) */
            @apply shadow-xl transition duration-300 hover:shadow-2xl h-full flex flex-col overflow-hidden rounded-xl;
        }
        h1, h2, h3 { color: var(--primary-dark-blue); }
        .text-soft-text { color: var(--soft-text); }

        /* تنسيق زر البطل (hero-btn) */
        .hero-btn {
            @apply px-4 py-2 rounded-lg text-sm font-medium transition duration-300 mt-auto; /* تنسيقات أساسية للزر */
            background-color: var(--accent-blue);
            color: var(--primary-dark-blue);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
        }
        .hero-btn:hover {
            opacity: 0.9;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.1);
        }
        html.dark .hero-btn {
            background-color: var(--accent-blue);
            color: #1A202C; /* كحلي داكن */
        }
        
        /* تنسيقات العناصر المتحركة (animated-circles-container) - غير مستخدمة هنا لكن تم الإبقاء عليها */
        .animated-circles-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: -1; /* يوضع خلف المحتوى */
        }
        .animated-circles-container::before {
            content: '';
            position: absolute;
            width: 400px;
            height: 400px;
            background: rgba(6, 182, 212, 0.1); /* accent-blue/10% */
            border-radius: 50%;
            top: -100px;
            right: -150px;
            animation: move-circle-1 20s infinite alternate;
        }
        .animated-circles-container::after {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            background: rgba(10, 47, 107, 0.05); /* primary-dark-blue/5% */
            border-radius: 50%;
            bottom: -50px;
            left: -100px;
            animation: move-circle-2 25s infinite alternate-reverse;
        }
        @keyframes move-circle-1 {
            from { transform: translate(0, 0) scale(1); }
            to { transform: translate(-50px, 50px) scale(1.1); }
        }
        @keyframes move-circle-2 {
            from { transform: translate(0, 0) scale(1.1); }
            to { transform: translate(70px, -30px) scale(1); }
        }
        html.dark .animated-circles-container::before { background: rgba(56, 189, 248, 0.15); }
        html.dark .animated-circles-container::after { background: rgba(144, 205, 244, 0.1); }
        
        /* تنسيق الروابط الاجتماعية في التذييل */
        .social-link {
            background-color: rgba(255, 255, 255, 0.1); 
        }

        /* تنسيق خاص لبطاقة الفعاليات السابقة لتشبه التصميم السابق */
        .past-event-card {
            @apply border-t-2 border-gray-300 dark:border-gray-600; 
        }
    </style>
</head>
<body class="text-main min-h-screen flex flex-col">
    
    <%- include('partials/header', { isLoggedIn: isLoggedIn, isAdmin: isAdmin, currentPage: 'events' }) %>

    <main class="flex-grow container mx-auto p-4 md:p-8 mt-16">
        <header class="text-center mb-12">
            <h1 class="text-4xl font-extrabold text-[var(--primary-dark-blue)] dark:text-[var(--accent-blue)] mb-2">
                <i class="fas fa-calendar-alt ml-2"></i> الدورات والفعاليات
            </h1>
            <p class="text-lg text-soft-text">
                انضم إلينا في فعالياتنا القادمة أو استعرض إنجازاتنا السابقة.
            </p>
        </header>
        
        <section class="mb-16">
            <h2 class="text-3xl font-bold mb-6 text-[var(--main-text)] border-b pb-2 border-gray-200 dark:border-gray-700">
                <i class="fas fa-calendar-check ml-2 text-[var(--accent-blue)]"></i> فعاليات قادمة
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% if (false) { %>
                    <% } %>
            </div>
        </section>

        <section class="mb-12">
            <h2 class="text-3xl font-bold mb-6 text-[var(--main-text)] border-b pb-2 border-gray-200 dark:border-gray-700">
                <i class="fas fa-history ml-2 text-soft-text"></i> فعاليات سابقة
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
                <% if (pastEvents && pastEvents.length > 0) { %>
                    <% pastEvents.forEach(event => { %>
                        <div class="bg-card past-event-card">
                            <% if (event.imageUrl) { %>
                                <div class="h-40 overflow-hidden">
                                    <img src="<%= event.imageUrl %>" alt="<%= event.name %>" class="w-full h-full object-cover transform hover:scale-105 transition duration-500">
                                </div>
                            <% } %>

                            <div class="p-4 md:p-6 flex flex-col flex-grow">
                                <h3 class="text-xl font-bold mb-3 text-[var(--main-text)]"><%= event.name %></h3>
                                
                                <div class="space-y-1 mb-3">
                                    <div class="flex items-center text-sm text-soft-text">
                                        <i class="fas fa-calendar-day ml-2 text-soft-text"></i>
                                        <span>التاريخ: <%= new Date(event.date).toLocaleDateString('ar-EG', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                                    </div>
                                    <div class="flex items-center text-sm text-soft-text">
                                        <i class="fas fa-users ml-2 text-soft-text"></i>
                                        <span>الحاضرون: <span class="font-semibold"><%= event.attendeesCount || 'غير متوفر' %></span></span>
                                    </div>
                                </div>
    
                                <p class="text-soft-text my-2 flex-grow"><%= event.description.substring(0, 80) %>...</p>
    
                                <a href="/archive/events/<%= event._id %>" class="mt-4 text-center text-sm font-semibold text-[var(--primary-dark-blue)] hover:underline">
                                    مشاهدة الأرشيف <i class="fas fa-chevron-left mr-1"></i>
                                </a>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="col-span-full text-center p-8 bg-card rounded-xl shadow-lg text-soft-text">
                        <p class="text-xl font-semibold">لا توجد فعاليات سابقة لعرضها حالياً.</p>
                    </div>
                <% } %>
            </div>
        </section>
    </main>

    <%- include('partials/footer') %>

    <script>
        const themeToggleDesktop = document.getElementById('theme-toggle-desktop');
        const themeToggleMobile = document.getElementById('theme-toggle-mobile');
        const htmlElement = document.documentElement;

        const savedTheme = localStorage.getItem('theme');
        if (savedTheme) {
            htmlElement.classList.remove('light', 'dark');
            htmlElement.classList.add(savedTheme);
        } else {
            htmlElement.classList.add('light'); 
        }

        const toggleTheme = () => {
            if (htmlElement.classList.contains('light')) {
                htmlElement.classList.remove('light');
                htmlElement.classList.add('dark');
            } else {
                htmlElement.classList.remove('dark');
                htmlElement.classList.add('light');
            }
            
            const newTheme = htmlElement.classList.contains('dark') ? 'dark' : 'light';
            localStorage.setItem('theme', newTheme);
        };
        if(themeToggleDesktop) themeToggleDesktop.addEventListener('click', toggleTheme);
        if(themeToggleMobile) themeToggleMobile.addEventListener('click', toggleTheme); // ✅ تم التصحيح
        const button = document.getElementById('mobile-menu-button');
        const menu = document.getElementById('mobile-menu');

        if(button && menu) {
            button.addEventListener('click', () => {
                menu.classList.toggle('hidden');
            });
        }
    </script>
</body>
</html>