<!DOCTYPE html>
<html lang="ar" dir="rtl" class="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>النادي الهندسي | تسجيل عضوية</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;800;900&display=swap" rel="stylesheet">
    
    <style>
        /* ==================================== */
        /* تعريف الألوان والتنسيقات الموحدة */
        /* ==================================== */
        :root {
            --primary-dark-blue: #0A2F6B;
            --accent-blue: #3498DB; 
            --light-bg: #F0F2F5; 
            --main-bg: #FFFFFF; 
            --main-text: #333333;
        }
        html.dark {
            --light-bg: #1A202C;
            --main-bg: #2D3748;
            --main-text: #E2E8F0;
            --primary-dark-blue: #90CDF4; 
            --accent-blue: #90CDF4; 
        }
        body { 
            font-family: 'Tajawal', sans-serif;
            background-color: var(--light-bg); 
            color: var(--main-text); 
            transition: background-color 0.3s, color 0.3s;
        }
        h1, h2, h3 { color: var(--primary-dark-blue);
        }
        .bg-card { background-color: var(--main-bg); color: var(--main-text);
        }
        .icon-shadow { filter: drop-shadow(0 4px 4px rgba(0,0,0,0.2));
        }
        
        /* تنسيقات الأزرار الرئيسية */
        .hero-btn {
            background-color: var(--accent-blue);
            color: #FFFFFF; 
            box-shadow: 0 0px 15px -3px rgba(0, 0, 0, 0.4), 0 4px 6px -2px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }
        html.dark .hero-btn {
            background-color: var(--accent-blue);
            color: var(--primary-dark-blue);
        }
        .social-link {
            background-color: var(--primary-dark-blue);
            color: #FFFFFF;
        }
        html.dark .social-link {
            background-color: var(--main-bg);
            color: var(--primary-dark-blue);
        }
        /* تعديلات ألوان شريط التنقل */
        .nav-link {
            color: var(--primary-dark-blue);
        }
        html.dark .nav-link {
            color: var(--main-text);
        }
        /* لون شريط التنقل في الوضع الداكن ليعود إلى الأزرق الداكن الثابت */
        html.dark .nav-dark-mode-bg {
            background-color: #0A2F6B;
            border-bottom-color: transparent;
        }
    </style>
</head>
<body class="text-main min-h-screen flex flex-col">
    
    <%- include('partials/header', { isLoggedIn: isLoggedIn, isAdmin: isAdmin, currentPage: 'register' }) %>

    <main class="flex-grow py-16 px-4">
        <div class="max-w-xl mx-auto bg-card p-8 md:p-10 rounded-xl shadow-2xl border-t-4 border-accent-blue">
            <h1 class="text-3xl font-extrabold text-center mb-6">تسجيل عضوية جديدة</h1>
            <p class="text-center mb-8 text-gray-600 dark:text-gray-300">املأ الحقول التالية للانضمام إلى النادي الهندسي.</p>

        
            <form action="/register" method="POST" class="space-y-6 text-right">
                
                <div>
                    <label for="full-name" class="block text-sm font-medium text-main mb-1">الاسم الكامل</label>
                    <input type="text" id="full-name" name="name" required 
                                class="w-full mt-1 p-3 border border-gray-300 rounded-lg focus:ring-accent-blue focus:border-accent-blue dark:bg-gray-700 dark:border-gray-600 dark:text-white transition duration-300">
                </div>
                
                <div>
                    <label for="student-id" class="block text-sm font-medium text-main mb-1">الرقم الجامعي</label>
                    <input type="text" id="student-id" name="student_id" required pattern="[0-9]{8,}" 
                                class="w-full mt-1 p-3 border border-gray-300 rounded-lg focus:ring-accent-blue focus:border-accent-blue dark:bg-gray-700 dark:border-gray-600 dark:text-white transition duration-300">
                </div>

                <div>
                    <label for="email" class="block text-sm font-medium text-main mb-1">البريد الإلكتروني</label>
                    <input type="email" id="email" name="email" required 
                                class="w-full mt-1 p-3 
                                border border-gray-300 rounded-lg focus:ring-accent-blue focus:border-accent-blue dark:bg-gray-700 dark:border-gray-600 dark:text-white transition duration-300">
                </div>

                <div>
                    <label for="major" class="block text-sm font-medium text-main mb-1">التخصص الأكاديمي</label>
                    <select name="department" id="major" required 
                                class="w-full mt-1 p-3 border border-gray-300 rounded-lg focus:ring-accent-blue focus:border-accent-blue dark:bg-gray-700 dark:border-gray-600 dark:text-white transition duration-300">
                        <option value="" disabled selected>اختر التخصص</option>
                        <option value="هندسة عامة">هندسة عامة</option>
                        <option value="هندسة مدنية">هندسة مدنية</option>
                        <option value="هندسة كهربائية">هندسة كهربائية</option>
                        <option value="هندسة ميكانيكية">هندسة ميكانيكية</option>
                        <option value="هندسة معمارية وتصميم داخلي">هندسة معمارية وتصميم داخلي</option>
                    </select>
                </div>
                
                <div>
                    <label for="password" class="block text-sm font-medium text-main mb-1">كلمة المرور</label>
                    <input type="password" id="password" name="password" required 
                                class="w-full mt-1 p-3 border border-gray-300 rounded-lg focus:ring-accent-blue focus:border-accent-blue dark:bg-gray-700 dark:border-gray-600 dark:text-white transition duration-300">
                </div>

                <button 
                    type="submit" 
                    class="hero-btn w-full py-3 mt-4 text-xl font-bold rounded-lg hover:bg-blue-600 transform hover:-translate-y-0.5">
                    تسجيل
                </button>
            </form>
            
            <div class="mt-8 text-center text-sm text-gray-500 dark:text-gray-400">
                لديك حساب بالفعل؟
                <a href="/login" class="text-accent-blue hover:underline font-semibold">تسجيل الدخول</a>
            </div>
        </div>
    </main>
    
    <%- include('partials/footer') %>

    <script>
        // كود لتغيير الوضع الداكن/الفاتح وتفعيل قائمة الجوال 
        const themeToggleDesktop = document.getElementById('theme-toggle-desktop');
        const themeToggleMobile = document.getElementById('theme-toggle-mobile');
        const htmlElement = document.documentElement;

        // [التحميل]: تمكين التسلسل في هذه الصفحة
        const savedTheme = localStorage.getItem('theme');
        if (savedTheme) {
            htmlElement.classList.remove('light', 'dark');
            htmlElement.classList.add(savedTheme);
        } else {
             htmlElement.classList.add('light');
        // تعيين الافتراضي إذا لم يوجد
        }

        // وظيفة تبديل الوضع وحفظه
        const toggleTheme = () => {
            let currentTheme = htmlElement.classList.contains('dark') ?
            'dark' : 'light';
            let newTheme = currentTheme === 'light' ? 'dark' : 'light';

            htmlElement.classList.remove(currentTheme);
            htmlElement.classList.add(newTheme);
        // [الحفظ]: حفظ النمط الجديد
            localStorage.setItem('theme', newTheme); 
        };
        if(themeToggleDesktop) themeToggleDesktop.addEventListener('click', toggleTheme);
        if(themeToggleMobile) themeToggleMobile.addEventListener('click', toggleTheme);
        
        // تفعيل قائمة الجوال
        const button = document.getElementById('mobile-menu-button');
        const menu = document.getElementById('mobile-menu');

        if(button && menu) {
            button.addEventListener('click', () => {
                menu.classList.toggle('hidden');
            });
        }
    </script>
</body>
</html>